// Logic Building Coding Questions

// You can create a JavaScript function called reverseString to reverse the characters of a given string. Here's how you can implement it:

// javascript
function reverseString(str) {
  // Split the string into an array of characters, reverse it, then join it back into a string
  return str.split("").reverse().join("");
}

// // Example usage:
// console.log(reverseString("JavaScript")); // Output: "tpircSavaJ"
// console.log(reverseString("AlmaBetter University")); // Output: "ytisrevinU retteBamlA"
// This function splits the input string into an array of individual characters, then reverses the order of the characters using the reverse() method, and finally joins the characters back into a string using the join() method.




